# ⚠️ SECURITY: Never commit this file to version control
# Add backend/.env to .gitignore

# Database Configuration
# CRITICAL: Update these credentials immediately if they were exposed
DATABASE_URL=postgresql://username:password@host:5432/database_name

# JWT Secret
# Generate a new secure secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-generated-secure-jwt-secret-here

# Server Configuration
PORT=3001

# CORS Origins (Your frontend URL)
CORS_ORIGIN=http://localhost:5173
